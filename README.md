# Educational Platform (LMS) API

Учебная платформа на Spring Boot. Реализована система управления пользователями, курсами и запись студентов на курсы.
Выполнен в рамках итогового проекта по модулю "ORM-фреймворки для Java".

## 🛠 Технологии
*   **Java 21**
*   **Spring Boot 3.x**
*   **Spring Data JPA (Hibernate)**
*   **PostgreSQL**
*   **Maven**

## 🚀 Инструкция по запуску

1.  **База данных**
    Убедитесь, что у вас установлен PostgreSQL. Создайте пустую базу данных:
    ```bash
    createdb lms_db
    ```

2.  **Запуск приложения**
    Откройте терминал в папке проекта и выполните команду:
    ```bash
    ./mvnw spring-boot:run
    ```

Приложение запустится на порту **9090**.
Адрес API: `http://localhost:9090`

## 🔌 API Эндпоинты

### 👤 Пользователи (Users)
*   **GET** `/api/users` — Получить список всех пользователей.
*   **POST** `/api/users` — Создать нового пользователя.
    *   *Пример тела запроса (JSON):*
        ```json
        {
          "name": "Maria",
          "email": "maria@example.com",
          "role": "STUDENT"
        }
        ```

### 📚 Курсы (Courses)
*   **GET** `/api/courses` — Получить список всех курсов.
*   **POST** `/api/courses` — Создать новый курс.
    *   *Пример тела запроса (JSON):*
        ```json
        {
          "title": "Java Spring Boot",
          "description": "Full course for beginners"
        }
        ```

### 🎓 Запись на курс (Enrollment)
Главная логика приложения. Связывает студента с курсом.

*   **POST** `/api/courses/{courseId}/enroll/{studentId}`
    *   *Параметры передаются в URL.*
    *   *Пример:* `/api/courses/1/enroll/2` (Записать студента с ID 2 на курс с ID 1).

---

После записи на курс, при запросе пользователя (`GET /api/users`), в поле `courses` отобразится список курсов, на которые он записан.
